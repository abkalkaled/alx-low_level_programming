#include <stdio.h>
/**
 * main - entry point of code
 *
 * Return: returns value of zero
 */
int main(void)
{
	int n = 0;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
